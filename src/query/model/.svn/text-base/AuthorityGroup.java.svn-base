package query.model;

import java.util.HashMap;

public class AuthorityGroup {

	private HashMap<String, Authority> authoritys = new HashMap<String, Authority>();

	public void addAuthority(String key, Authority authority) {
		authoritys.put(key, authority);
	}

	public HashMap<String, Authority> getAuthorities() {
		return authoritys;
	}

}
